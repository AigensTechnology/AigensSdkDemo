pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
        jcenter()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        jcenter()
    }
}
rootProject.name = "AigensSdkDemo"
include ':app'

//these need to move to real sdk
include ':aigens-sdk-core'
project(':aigens-sdk-core').projectDir = new File(settingsDir, '/Users/peter/workspace/aigens-mono/plugins/aigens-sdk-core/android')
